rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default: deny all read/write access
    // Collections will be added as features are implemented
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
