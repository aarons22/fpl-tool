# Example environment configuration for local Firebase emulator testing
# Copy this file to .env.local (not .env.yaml) and fill in your actual values
# .env.local is gitignored and should NEVER be committed
#
# NOTE: For local emulators, use .env.local with KEY=value format
#       For GitHub Actions deployment, .env.yaml with KEY: value format is created automatically

# The Odds API - Get your key at https://the-odds-api.com/
ODDS_API_KEY=your-odds-api-key-here
